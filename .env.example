# ╔══════════════════════════════════════════════════════════════════╗
# ║                        uloggd — .env                              ║
# ║                                                                  ║
# ║  Copie este arquivo como .env e preencha com suas credenciais.    ║
# ║  Nunca commite o arquivo .env no repositório.                      ║
# ╚══════════════════════════════════════════════════════════════════╝


# ─── App Base URL ──────────────────────────────────────────────────
# URL base da aplicação (usada em OAuth redirects e APIs)
# Ex:
# Dev: http://localhost:3000
# Prod: https://seusite.com

APP_URL=


# ─── Steam API ─────────────────────────────────────────────────────
# Crie sua API key em: https://steamcommunity.com/dev/apikey
# Usada para acessar dados públicos da Steam (perfil, jogos, etc).

STEAM_WEB_API_KEY=


# ─── Twitch / IGDB ────────────────────────────────────────────────
# Crie uma aplicação em https://dev.twitch.tv/console/apps
# Essas credenciais são usadas para autenticar na API do IGDB.

TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
TWITCH_REDIRECT_URI=


# ─── Supabase (Client) ───────────────────────────────────────────
# Variáveis expostas ao frontend (prefixo VITE_).
# Encontre em: Supabase Dashboard → Settings → API
# A anon key é pública, mas o acesso é controlado por RLS.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=


# ─── Supabase (Server) ───────────────────────────────────────────
# Variáveis exclusivas do backend. NUNCA exponha a service role key.
# A service role key ignora todas as políticas de RLS.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=


# ─── Imgchest ────────────────────────────────────────────────────
# Usada para upload de imagens (banners, etc).
# Obtenha sua API key em https://imgchest.com/docs/api

IMGCHEST_API_KEY=

# ─── Discord ─────────────────────────────────────────────────────
# Webhook para notificações da equipe (solicitações de verificação, etc).
# Crie em: Discord → Canal → Editar Canal → Integrações → Webhooks

DISCORD_WEBHOOK_URL=
