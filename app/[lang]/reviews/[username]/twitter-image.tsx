// Same card as Open Graph. Kept as a re-export rather than a second
// implementation, since a card that differs between platforms is a card
// nobody notices is wrong.
export { default, alt, size, contentType } from "./opengraph-image";
