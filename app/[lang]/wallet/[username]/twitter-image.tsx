// The wallet is the same public collection on every sharing platform. Keeping
// one renderer also guarantees that its counts and rarity never drift apart.
export { default, alt, size, contentType } from "./opengraph-image";
